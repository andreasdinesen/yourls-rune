# Send the bare site root to the admin dashboard. A URL shortener has no public
# landing page, and YOURLS ships no index at the webroot root, so without this
# "/" returns 403 (no DirectoryIndex). Short URLs like /abc are unaffected: the
# match is anchored to exactly "/".
RedirectMatch 302 ^/$ /admin/
